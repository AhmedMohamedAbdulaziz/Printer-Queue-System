# Printer-Queue-System
You are asked to build a Printer Queue Simulation System that manages print jobs in a queue. Users can add new print jobs, view the waiting list, print the next document, undo the last print, and view the printing history — all through a well-designed console interface 
